__pycache__/
*.py[cod]
*.egg-info/

# Local run outputs.
logs/

# Model checkpoints and local download scratch (large, never commit).
checkpoints/
downloads/

# RLinf is consumed as an external checkout, not vendored here.
/rlinf/
/rlinf.egg-info/

/resources/

uv.lock

# Documentation build artifacts and local environment.
.docs-venv/
docs/build/

build/
.claude/settings.json
artifacts/

# Local runtime scratch / state (never commit).
.gap_run/
MUJOCO_LOG.TXT

# Credentialed launcher scripts — contain API keys, never commit.
harness_auto.sh
harness_run.sh

# Transfer artifacts (reproducible from the repo / regenerable).
rpent-data.bundle
rpent-logs.tar.zst
# Data-transfer clone of this repo (nested, has its own .git). Anchored so it
# never hides the source package rpent/memory/ (see note in commit history).
/rpent/rpent/
.claude/
